{
  "name": "pdf-maker-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Telegram bot that turns photos into a single PDF, hosted on Netlify",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "set-webhook": "node scripts/set-webhook.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "@netlify/functions": "^2.8.2",
    "@types/node": "^22.20.3",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
